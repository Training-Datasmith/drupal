# Themes are not supposed to provide/install this kind of config normally.
# This exists for testing purposes only.
# @see \Drupal\Kernel_Tests\Core\Theme\Theme_Installer_Test
langcode: en
status: true
dependencies:
  enforced:
    theme:
      - test_base_theme
id: fancy
label: 'Fancy date'
locked: false
pattern: U
